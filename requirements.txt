# <2 deliberately: mcp 2.0 relocated mcp.server.fastmcp. Moving to it is a
# separate change, not something an unpinned build should do on its own.
mcp[cli]>=1.0.0,<2.0.0
httpx>=0.27.0
markdownify>=0.13.1
cachetools>=5.3.0
selectolax>=0.3.21
